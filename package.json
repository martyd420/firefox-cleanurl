{
  "name": "cleanurl",
  "version": "1.0.0",
  "description": "Firefox extension that removes or replaces tracking parameters in URLs before navigation.",
  "private": true,
  "scripts": {
    "test": "node --test",
    "lint": "web-ext lint",
    "build": "web-ext build --overwrite-dest",
    "start": "web-ext run"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "web-ext": "^8.0.0"
  }
}
